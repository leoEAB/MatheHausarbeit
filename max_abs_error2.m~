function [max_error] = max_abs_err(f,n,f_loesung)
    k = length(n);
    max_error = zeros(k,1);
    
    
   
    
    
        [~, y] = f(n);
        [~, y_loesung] = f_loesung(n*10);
        y
        max_error(i) = max(abs(y - f_loesung(t)));    
    end



end